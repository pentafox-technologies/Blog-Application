apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  importDerivedRoles:
    - common_roles
  resource: "category"
  rules:
    - actions: ['create']
      effect: EFFECT_ALLOW
      derivedRoles:
        - admin

    - actions: ['update']
      effect: EFFECT_ALLOW
      derivedRoles:
        - admin

    - actions: ['delete']
      effect: EFFECT_ALLOW
      derivedRoles:
        - admin
